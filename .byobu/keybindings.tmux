set -g prefix F12
unbind-key -n C-a

unbind-key -n F1
unbind-key -n C-S-F12

unbind-key -n S-Up
unbind-key -n S-Down
unbind-key -n S-Left
unbind-key -n S-Right

unbind-key -n M-Up
unbind-key -n M-Down
unbind-key -n M-Left
unbind-key -n M-Right
